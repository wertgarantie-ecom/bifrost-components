import {css} from "lit-element";

// language=CSS
export const landingPageStylingGeneral = css`
    :host {
        font-family: var(--wertgarantie-landing-page-font-family, "Helvetica", Arial, sans-serif);
    }

    .landing-page {
        max-width: 1200px;
        margin: auto;
    }
`;